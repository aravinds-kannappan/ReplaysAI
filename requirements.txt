# Serverless API (Vercel) and local backend dev.
# Heavy offline ingestion/CV dependencies live in requirements-ingestion.txt —
# keeping them out of this file keeps the Vercel Python function under the
# unzipped size limit.

# Web framework
fastapi==0.115.0
uvicorn==0.30.6

# Cache (optional — endpoints degrade gracefully without REDIS_URL)
redis==5.1.1

# Auth
PyJWT==2.10.1

# HTTP client (ESPN public API, YouTube search)
httpx==0.27.2
certifi>=2024.7.4

# AI assistant (lazily imported when API keys are configured)
anthropic==0.37.1
openai==1.58.1

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
